using UnityEngine;
using System.Collections;

public class Documentation : MonoBehaviour {

	/*
		1) Astar Manager object with grid has to be at 0,0
		2) Tag Astar GO as Astar
		3) Terrain has to be offseted so that its center is at 0,0
		4) Setup Grid, world size same as Terrain + some offset, Unwalkable Mask = Obstacle, Terrain Mask = Ground, nodeRadius default = 120 ( can be adjusted )
		5) Terrain tag and layer as Ground, 
		6) Obstacles tag and layer as Obstacle
	 */
}
